# ѕарсер книг с сайта [tululu.org](https://tululu.org)


ƒанный проект предназначен дл€ скачивани€ книг из большой библиотеки. ѕроект также помогает загрузить вместе с книгой:

1. јвтора книги
2.  омментарии пользователей
3. ∆анр книги
4. ќбложку книги

##  ак установить

Python3 должен быть уже установлен. «атем используйте pip (или pip3, если есть конфликт с Python2) дл€ установки зависимостей:

```pip install -r requirements.txt```

##  ак запустить скрипт

ƒл€ того, чтобы скачать книги, необходимо прописать команду в терминал:

```python main.py "указать диапозон книг"```

Ќапример:
```python main.py --start_id 20 --end_id 30```
- данна€ команда произведет загрузку 10 книг с id книги от 20 до 30 включительно.

≈сли необходимо скачать сразу много страниц с книгами:

```python main.py "указать диапозон страниц"```

Ќапример:

```python main.py --start_page 20 --endpage 30```
- данна€ команда произведет загрузку книг со страницы 20 до 29 включительно.
- если не указать параметр --endpage, то книги загруз€тс€ с 20 и по последнюю страницу(701)

≈сть дополнительные параметры, которые можно использовать с командами выше:
- --skip_imgs позвол€ет скачать все, кроме картинок
- --skip_txt позвол€ет не скачивать текст книги, а только основную информацию о ней
- --dest_folder при указании данной команды указываетс€ путь, куда вы хотите скачать файлы

Ќапример:

```python main.py --start_page 20 --endpage 30 --skip_imgs```
команда скачать все книги с 20 по 29 страницу, но не загрузит картинки дл€ них

```python main.py --start_page 20 --endpage 30 --skip_txt```
команда скачать всю информацию о книгах с 20 по 29 страницу, но сам текст скачан не будет

```python main.py --start_page 20 --endpage 30 --dest_folder boooks/```
команда скачать все книги с 20 по 29 страницу и загрузить их в указанную вами папку boooks(default=books)

## ÷ель проекта

 од написан в образовательных цел€х на онлайн-курсе дл€ веб-разработчиков [dvmn.org](https://dvmn.org).